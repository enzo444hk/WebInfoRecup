{
  "name": "veridia-portail",
  "version": "1.1.0",
  "description": "Portail de connexion + vérification capteurs + journal live + envoi email quotidien des logs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "nodemailer": "^6.9.16",
    "node-cron": "^3.0.3"
  }
}
